apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: essay-analyzer
  annotations:
    run.googleapis.com/launch-stage: BETA
spec:
  template:
    metadata:
      annotations:
        run.googleapis.com/container-dependencies: '{"frontend":["api-server"], "api-server":["adk-api"]}'
    spec:
      containerConcurrency: 80
      timeoutSeconds: 300
      containers:
        # Ingress Container: Frontend (Nginx proxy)
        - image: us-central1-docker.pkg.dev/adk-essay-analyser-gaa/adk-repo/frontend:20260321174029
          name: frontend
          ports:
            - containerPort: 8080
          startupProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 6
          resources:
            limits:
              cpu: 1000m
              memory: 512Mi

        # Sidecar 1: Node.js API Server
        - image: us-central1-docker.pkg.dev/adk-essay-analyser-gaa/adk-repo/api-server:20260321174029
          name: api-server
          env:
            - name: PORT
              value: "3001"
            - name: ADK_API_URL
              value: "http://localhost:8000"
            - name: NODE_ENV
              value: "production"
          startupProbe:
            httpGet:
              path: /api/health
              port: 3001
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 10
          resources:
            limits:
              cpu: 1000m
              memory: 1024Mi

        # Sidecar 2: Python ADK API
        - image: us-central1-docker.pkg.dev/adk-essay-analyser-gaa/adk-repo/adk-api:20260321174029
          name: adk-api
          env:
            - name: PORT
              value: "8000"
            - name: HOST
              value: "0.0.0.0"
            - name: GOOGLE_GENAI_API_KEY
              value: "AIzaSyDgHEkeFV36q-E7gWC49NbVcveop0ecWo4"
            - name: GOOGLE_API_KEY
              value: "AIzaSyDgHEkeFV36q-E7gWC49NbVcveop0ecWo4"
          startupProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 15
          resources:
            limits:
              cpu: 1000m
              memory: 2048Mi
